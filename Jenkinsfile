pipeline {
    agent any
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello world!' 
                sh 'ls -a roma'
                sh 'cat Jenkinsfile'
                sh 'ls -a '
		sh 'hostnames'
		sh 'ip addr'
            }
        }
    }
}
