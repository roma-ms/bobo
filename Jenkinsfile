pipeline {
    agent localhost
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello world!' 
                sh 'ls -a roma'
                sh 'cat  bobo/ari.txt'
                sh 'ls -a bobo'
            }
        }
    }
}
