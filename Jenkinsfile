pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo 'Hello world!' 
                sh 'ls -a roma'
                sh ' touch bobo/ari.txt'
                sh 'ls -a bobo'
            }
        }
    }
}
